# Legacy example only. Smart Router v3 does not set a Root model.
# Select Sol from the Codex conversation model picker if Sol should be the Root
# for a particular conversation.

[agents]
enabled = true
max_concurrent_threads_per_session = 4
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "medium"
